## Summary

<!-- One paragraph describing the change. -->

## Why

<!-- Motivation, context, links to issues. Don't repeat the diff. -->

## Behaviour changes

<!-- New env vars, removed flags, status codes, breaking changes for downstream users. "None" if purely internal. -->

## Checklist

- [ ] One logical change in this PR
- [ ] `npm run lint && npm test && npm run build` clean locally
- [ ] PR has a **type** label (`feature` / `bug` / `chore` / `refactors` / `docs` / `perf` / `tests` / `maintenance`)
- [ ] If targeting `main` for release: also has `release` + one of `major` / `minor` / `patch`
- [ ] Docs updated (README, env table, CONTRIBUTING) if behaviour changed
